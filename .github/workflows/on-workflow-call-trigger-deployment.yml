name: Trigger Deployment

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
        description: "Environment to deploy to (preview or production)"

jobs:
  deploy:
    environment: ${{ inputs.environment }}
    name: Trigger ${{ inputs.environment }} Deployment
    runs-on: ubuntu-latest
    timeout-minutes: 1
    concurrency:
      group: ${{ inputs.environment }}-deploy
      cancel-in-progress: false
    steps:
      - name: Send Notification
        uses: actions/github-script@v7
        with:
          script: |
            const environment = "${{ inputs.environment }}";
            core.info(`Deploying to ${environment}`);

      - name: Success
        run: |
          echo "::notice title=${{ inputs.environment }} Deploy::Deployment started by @${{ github.actor }} at $(date)"
          echo "Deployment triggered successfully"
